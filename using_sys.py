# sys refers the current operating system
import sys

# we can access a list of every location that Python will look for imports
sys.path.append('d:/utilitiesGoHere') # we can add locations where Python should look for imports
print(sys.path)